const Footer = () => {
  return (
    <footer className="flex items-center justify-center bg-primary-2  text-zinc-50 p-2">
      &copy; {new Date().getFullYear()} | Caio Martins
    </footer>
  );
};

export default Footer;
